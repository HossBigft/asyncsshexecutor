To spin up 6 test containers, 5 is default. 
```
start-ssh-containers.sh 10
```

To connect, `testpass` default password
```
testuser@localhost -p 2222
```

To test ssh connection, init venv
```
uv  sync
```

Then in root dir
```
python -m docker_testenv.test_ssh
```